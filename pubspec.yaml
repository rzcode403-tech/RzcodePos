name: supermarche_pos
description: SuperMarché POS — Flutter + SQLite
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.2
  path: ^1.9.0
  mobile_scanner: ^5.1.1
  intl: ^0.19.0
  provider: ^6.1.2
  pdf: ^3.10.8
  printing: ^5.12.0
  path_provider: ^2.1.2
  share_plus: ^9.0.0
  file_picker: ^8.0.0

flutter:
  uses-material-design: true
